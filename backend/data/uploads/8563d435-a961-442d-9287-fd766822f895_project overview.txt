┌────────────────────────────────────────────────────────────────────────┐
 │                      1. DOCUMENT INGESTION & OCR                       │
 │   - OCRService extracts text from PDF, images, or files.      │
 │   - OCRService computes metrics: ocr_confidence, pages, entities[cite: 4].│
 └───────────────────────────────────┬────────────────────────────────    │
                                     │
                                     ▼
 ┌────────────────────────────────────────────────────────────────────────┐
 │                        2. RAG KNOWLEDGE RETRIEVAL                      │
 │   - RAGKnowledgeService initializes in-memory Qdrant database.│
 │   - Generates embeddings using Gemini (`models/text-embedding-004`)[cite: 6].│
 │   - Seeds default policies and searches collection context[cite: 6].    │
 └───────────────────────────────────┬────────────────────────────────    │
                                     │
                                     ▼
 ┌────────────────────────────────────────────────────────────────────────┐
 │                  3. CLAUSE EXTRACTION & RISK EVALUATION                │
 │   - ClauseService retrieves grounding context via RAG service. │
 │   - Passes user role and document text into an enterprise prompt[cite: 1].│
 │   - Iterates through active Gemini model candidates[cite: 1].           │
 │   - Extracts structured JSON array containing clause types, confidence,│
 │     risk level, risk rationale, and RAG references[cite: 1].          │
 │   - Uses dynamic fallback evaluation if LLM calls fail[cite: 1].        │
 └───────────────────────────────────┬────────────────────────────────    │
                                     │
          ┌──────────────────────────┴──────────────────────────┐
          │                                                     │
          ▼                                                     ▼
 ┌───────────────────────────────┐                     ┌───────────────────────────────┐
 │ 4. ARCHIVE & HISTORY SIDEBAR  │                     │  5. LEGAL OPERATIONS DASHBOARD│
 │ - Fetches review history from │                     │ - Fetches operational metrics │
 │   `/api/v1/review/history`.                 │   from operations/metrics.│
 │ - Displays audit records with │                     │ - Displays throughput stats,  │
 │   file name, review action    │                     │   risk taxonomy (HIGH/MED/LOW),│
 │   (`ACCEPT`/`REJECT`), and    │                     │   governance actions, and     │
 │   timestamps[cite: 3].       │                     │   business unit breakdown[cite: 5].│
 └───────────────────────────────┘                     └───────────────────────────────┘
          │                                                     │
          └──────────────────────────┬──────────────────────────┘
                                     │
                                     ▼
 ┌────────────────────────────────────────────────────────────────────────┐
 │                        6. LEGAL CHAT ASSISTANT                         │
 │   - FastAPI router handles `/query` endpoint with `ChatRequest`.│
 │   - Formats recent conversational memory from chat history[cite: 2].  │
 │   - Retrieves matching RAG context using `rag_service.retrieve_context`[cite: 2].│
 │   - Invokes Gemini model candidates to generate answers and references[cite: 2].│
 └────────────────────────────────────────────────────────────────────────┘